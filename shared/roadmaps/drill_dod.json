{
  "_meta": {
    "note": "Drill track DoD — N seed → M EXACT conversion ratio tiers. ratio = EXACT_count / seed_count."
  },
  "track": "drill",
  "description": "Definition-of-Done tiers for the drill pipeline (smash→free→absolute chain). Each tier requires a minimum EXACT-yield ratio from seed input.",
  "updated": "2026-04-19",
  "tiers": {
    "scaffold_done": {
      "description": "Drill pipeline wired — blowup core + absolute module + seed_engine all callable. Yield ratio >= 0.05 (5 EXACT / 100 seeds).",
      "required": [
        "$NEXUS/shared/blowup/core/blowup.hexa",
        "$NEXUS/shared/blowup/modules/blowup_absolute.hexa",
        "$NEXUS/shared/blowup/seed/seed_engine.hexa"
      ],
      "smoke": []
    },
    "wired_done": {
      "description": "Chain runnable end-to-end, yield ratio >= 0.15. atlas.n6 receives EXACT appends.",
      "required": [
        "$NEXUS/shared/n6/atlas.n6",
        "$NEXUS/shared/n6/atlas.blowup.jsonl"
      ],
      "smoke": [
        "bash -c 'tail -2000 $NEXUS/shared/n6/atlas.n6 | grep -c EXACT | awk \"{ if (\\$1 >= 1) exit 0; else exit 1 }\"'"
      ]
    },
    "serving_done": {
      "description": "Mature drill — yield ratio >= 0.30 sustained over 3 successive runs, promotion to [11*] at least once.",
      "required": [
        "$NEXUS/shared/n6/atlas.n6"
      ],
      "smoke": [
        "bash -c 'grep -c \"\\[11\\*\\]\" $NEXUS/shared/n6/atlas.n6 | awk \"{ if (\\$1 >= 2) exit 0; else exit 1 }\"'"
      ]
    }
  }
}
